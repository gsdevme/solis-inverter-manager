# Secrets / local env
.env

# Build output
/bin/
/dist/
*.out
*.test
coverage.out

# Editor / OS
.DS_Store
.idea/
*.swp

# Python
# Bare "__pycache__/" matches at any depth; the old "*/__pycache__/*" only caught
# one level, so sidecar/tests/__pycache__ slipped through.
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.pylintrc

# Python virtualenv (sidecar, local only)
sidecar/.venv/
